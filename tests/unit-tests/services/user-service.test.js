test('create user with valid data', async () => {
    const userObj = {
        firstName: 'bisho',
        lastName: 'adly',
        email: 'bisho@mail.com',
        password: '12345'
    };
    // UserService.createNewUser(userObj);
});